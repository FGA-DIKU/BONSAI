# @package _global_
defaults: 
  - ../pretrain@
  - ../hardware/1gpu6cpu@hardware
  - _self_

training:
  max_len: 512
  batch_size: 2
  epochs: 10
  limit_val_batches: 10
  limit_train_batches: 10
  value_loss_weight: 1.0

model:
  _target_: bonsai.modules.networks.bonsai_nets.BonsaiValuePretrain
  value_embedding_mode: film
